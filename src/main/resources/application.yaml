

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/journal
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
server:
  port: 9091
